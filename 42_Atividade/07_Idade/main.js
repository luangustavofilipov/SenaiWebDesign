// Declarar a variável
let idade = Number(prompt("Informe a sua idade:"))

// Verificações de Idade
if (idade >= 18) {
    alert("Você é maior de idade!")
    document.write("Bem vindo!")
}
    else if (idade <= 17) {
        alert("Você é menor de idade!")
        document.write("Você foi desconectado!")
    }
