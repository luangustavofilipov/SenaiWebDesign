// Verificação do valor da comissão
function calcular() {// Declaração das Variáveis
    let peso = parseFloat(document.getElementById("peso").value)
    let quantidade = peso * 0.035

    alert(quantidade)
}